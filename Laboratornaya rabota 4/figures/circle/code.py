import math
default_radius = 5
def circle_perimeter(r = default_radius):
    perimetr = 2 * math.pi * r
    return perimetr
def circle_area(r = default_radius):
    area = math.pi * r**2
    return area
